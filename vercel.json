{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo \"Skipping install; deploying committed frontend/dist\"",
  "buildCommand": "echo \"Skipping build; deploying committed frontend/dist\"",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
